import React from 'react';
import ReactDOM from 'react-dom';
import WFLOPS from './wflops';

document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  ReactDOM.render(<WFLOPS/>, root);
});
